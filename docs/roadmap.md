# Roadmap — what we'd build next (deliberately deferred)

Shows "build now vs defer" judgment (job desc explicitly asks for this).

1. Notifications: email digests to parents (weekly summary).
2. File uploads for homework (PDF/photos) with signed URLs + AV scanning.
3. Ops/admin console: enrollment funnel triage, class scheduling, attendance.
4. Billing: self-serve paid enrollment (Stripe), proration, invoices.
5. Teacher analytics: cohort progress, assignment difficulty signals.
6. i18n (ID/EN), a11y pass, mobile PWA.
7. Migration path story: how this absorbs a messy founder-built schema (write a short ADR on data-integrity strategy — great interview talking point).
